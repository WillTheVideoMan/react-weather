/**
 * Stackable.js
 * Author: Will Hall
 *
 * Stackable --> A simple styled component that gives all child components absolute positioning through CSS
 */
import styled from "styled-components";

const Stackable = styled.div`
  * {
    position: absolute;
  }
`;

export default Stackable;
